schedules:
  - cron: "0 10 * * 6"
    always: true
    displayName: "Weekly Base Build - Ubuntu 24.04"
    branches:
      include:
        - main

pr: none
trigger: none

extends:
  template: template-buildbase.yaml
  parameters:
    ubuntu_version: '2404'
